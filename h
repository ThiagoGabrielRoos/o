// código omitido

async function redesFavoritasMundo() {
    const layout = {
        legend: {
        
        }
    }

    criarGrafico(data, layout)
}

redesFavoritasMundo()// código omitido

async function redesFavoritasMundo() {
    const layout = {
        legend: {
        
        }
    }

    criarGrafico(data, layout)
        
      incluirText('')
}

redesFavoritasMundo()async function redesFavoritasMundo() {
    const layout = {
        legend: {
        
        }
    }

    criarGrafico(data, layout)
        
      // incluirText('')
}

redesFavoritasMundo()// código omitido

function incluirTexto(texto) {

}

// código omitido// código omitido

const paragrafo = document.createElement('p');
paragrafo.classList.add('graficos-container_texto')
paragrafo.innerHTML = `Você sabia que o mundo tem <span>${pessoasNoMundo} bilhões</span> de pessoas e que aproximadamente <span>${pessoasConectadas} bilhões</span> estão conectadas em alguma rede social e passam em média <span>${horas} horas</span> e <span>${minutos} minutos</span> conectadas.<br>Isso significa que aproximadamente <span>${porcentagemConectada}%</span> de pessoas estão conectadas em alguma rede social.`;

// código omitido<!-- código omitido -->

<section id="graficos-container" class="graficos-container">
    <!-- Crie os gráficos/texto aqui -->
</section>

<!-- código omitido -->// código omitido

function incluirTexto(texto) {
    const container = document.getElementById('graficos-container')
}

// código omitidoconst paragrafo = document.createElement('p');
paragrafo.classList.add('graficos-container__texto')// código omitido

function incluirTexto(texto) {
    const container = document.getElementById('graficos-container')
    const paragrafo = document.createElement('p')
    paragrafo.classList.add('graficos-container__texto')
}

// código omitido// código omitido

function incluirTexto(texto) {
    const container = document.getElementById('graficos-container')
    const paragrafo = document.createElement('p')
    paragrafo.classList.add('graficos-container__texto')
    container.appendChild(paragrafo)
}

// código omitido// código omitido

function incluirTexto(texto) {
    const container = document.getElementById('graficos-container')
    const paragrafo = document.createElement('p')
    paragrafo.classList.add('graficos-container__texto')
    container.appendChild(paragrafo)
}

export { getCSS, tickConfig, criarGrafico, incluirTexto }import { getCSS, criarGrafico, incluirTexto } from "./common.js"

// código omitido// código omitido

async function redesFavoritasMundo() {
    const layout = {
        legend: {
        
        }
    }

    criarGrafico(data, layout)
        
     incluirTexto(`Embora o Instagram ocupe a quarta posição de redes sociais com mais usuários no mundo, ela é a rede social que as pessoas mais gostam. Seguida de WhatsApp e Facebook.`)
}

redesFavoritasMundo()// código omitido

async function redesFavoritasMundo() {
    const layout = {
        legend: {
        
        }
    }

    criarGrafico(data, layout)
        
     incluirTexto(`Embora o <span>Instagram</span> ocupe a quarta posição de redes sociais com mais usuários no mundo, ela é a rede social que as pessoas mais gostam. Seguida de WhatsApp e Facebook.`)
}

redesFavoritasMundo()// código omitido

async function redesFavoritasMundo() {
    const layout = {
        legend: {
        
        }
    }

    criarGrafico(data, layout)
        
     incluirTexto(`Embora o <span>Instagram</span> ocupe a quarta posição de redes sociais com mais usuários no mundo, ela é a rede social que as pessoas mais gostam. Seguida de WhatsApp e Facebook. <br> Continuo escrevendo…`)
}

redesFavoritasMundo()// código omitido

async function redesFavoritasMundo() {
    const layout = {
        legend: {
        
        }
    }

    criarGrafico(data, layout)
        
     incluirTexto(`Embora o <span>Instagram</span> ocupe a quarta posição em termos de número total de usuários entre as redes sociais, destaca-se como a <span>preferida pelos usuários</ span>. Supera até mesmo o <span>Facebook</span>, a plataforma com mais usuários, sendo a terceira opção mais apreciada pelos usuários. <br>Essa preferência evidencia a forte conexão e apreço que as pessoas têm pelo Instagram em comparação com outras redes sociais`)
}

redesFavoritasMundo()
